# ClawHub Appeal / 申诉说明 (h1b-finder)

**Subject: Clarification on h1b-finder skill metadata and ownership**

Dear ClawHub Administrators,

I am the publisher of the `h1b-finder` skill. I would like to clarify that the account **ewangchong** is the authorized official publisher for the domain **h1bfinder.com**.

The "Suspicious" flag was likely triggered by minor metadata inconsistencies in our initial v1.0.0 upload (specifically a typo in the homepage URL and a mismatch between the author handle and the branding).

**We have fully rectified these issues in v1.0.1:**
1. **Metadata Alignment**: `skill.json` and `SKILL.md` now both consistently point to **h1bfinder.com** as the author and homepage.
2. **Package Cleanup**: Removed all redundant internal directories (`inner/`, `testdir/`) to ensure a clean, standard skill structure.
3. **Safety Verified**: The skill only performs read-only queries to public labor disclosure data via our official API.

We request an manual review to clear the "Suspicious" status so that our users can safely automate their H1B research.

---

**申诉说明：关于 h1b-finder 技能元数据与所有权的澄清**

尊敬的 ClawHub 管理员：

我是 `h1b-finder` 技能的发布者。特此澄清，账号 **ewangchong** 是 **h1bfinder.com** 域名的唯一官方授权发布账号。

此前的“可疑（Suspicious）”标记可能是由 v1.0.0 版本中微小的元数据不一致导致的（主要是主页拼写错误以及作者 ID 与品牌口径的落差）。

**我们已在 v1.0.1 版本中完全修复了这些问题：**
1. **元数据对齐**：`skill.json` 与 `SKILL.md` 现已统一指向官方域名 **h1bfinder.com**。
2. **包体清理**：彻底删除了所有调试用的冗余目录（`inner/`, `testdir/`），确保结构纯净规范。
3. **安全合规**：该技能仅通过官方 API 对公开劳工披露数据进行只读查询，无任何隐私风险。

申请人工审核并解除“可疑”标记，以便我们的用户能够安全地使用 AI 自动化 H1B 调研。
